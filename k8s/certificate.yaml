apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: chatplan-cert
  namespace: pts-2026-rictjo
spec:
  secretName: chatplan-tls

  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt

  commonName: chatplan.icedc.se
  dnsNames:
    - chatplan.icedc.se
